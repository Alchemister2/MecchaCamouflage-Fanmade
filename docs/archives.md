# Archives

`archives/` contains code and notes that are useful for historical comparison but are not part of the active runtime.

- `archives/ue4ss/`: legacy UE4SS implementation, signatures, and old root scripts.
- `archives/notes/`: loose investigation notes moved out of the active runtime tree.

Build, deploy, and runtime scripts must not depend on `archives/`.
